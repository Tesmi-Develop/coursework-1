<Window x:Class="Coursework1.UI.ReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Параметры отчёта" Height="320" Width="500" 
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="ФИО водителя:" Grid.Row="0" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox x:Name="FullNameBox" Grid.Row="0" Grid.Column="1" Margin="10,5" Padding="3"/>

        <TextBlock Text="Сумма (₽):" Grid.Row="1" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox x:Name="MinAmountBox" Grid.Row="1" Grid.Column="1" Margin="10,5" Padding="3" 
                 Width="150" HorizontalAlignment="Left"/>

        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,15,0,5">
            <TextBlock Text="Дата нарушений (включительно):" Margin="0,0,0,8" Foreground="#333"/>
            
            <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                <DatePicker x:Name="StartDatePicker" Width="150" VerticalAlignment="Center"/>
                <TextBlock Text="—" VerticalAlignment="Center" Margin="10,0"/>
                <DatePicker x:Name="EndDatePicker" Width="150" VerticalAlignment="Center"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Отмена" Width="80" IsCancel="True" Margin="0,0,10,0"/>
            <Button Content="Сформировать" Width="120" IsDefault="True" Click="GenerateReport_Click"/>
        </StackPanel>
    </Grid>
</Window>