<UserControl x:Class="Coursework1.UI.FormattedDatePicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Coursework1.UI.Components"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="200">
    
    <Grid>
        <DatePicker x:Name="InternalPicker" 
                    SelectedDate="{Binding SelectedDate, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}">
            <DatePicker.Resources>
                <Style TargetType="DatePickerTextBox">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </Style>
            </DatePicker.Resources>
        </DatePicker>
        
        <TextBox x:Name="CustomInputBox"
                 VerticalContentAlignment="Center"
                 Margin="0,0,25,0" 
                 Padding="4,0,0,0"
                 Text="{Binding InputText, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=LostFocus}"
                 LostFocus="CustomInputBox_LostFocus"
                 PreviewKeyDown="CustomInputBox_PreviewKeyDown"
                 MouseLeftButtonDown="CustomInputBox_MouseLeftButtonDown">
            
            <TextBox.Style>
                <Style TargetType="TextBox">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    <Setter Property="FontFamily" Value="{Binding FontFamily, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    <Setter Property="FontSize" Value="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    <Setter Property="Cursor" Value="IBeam"/>
                </Style>
            </TextBox.Style>
        </TextBox>
    </Grid>
</UserControl>